<form method="post" action="{{ path('employe_delete', {'id': employe.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer le fichier?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employe.id) }}">
    <button class="btn">Supprimer</button>
</form>